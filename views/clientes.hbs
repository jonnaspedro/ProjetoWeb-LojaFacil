<section class="page-content">
  <h1>Clientes Cadastrados</h1>

  <form action="/clientes" method="POST" class="form-cliente">
    <input type="text" name="nome" placeholder="Nome do Cliente" required>
    <button type="submit" class="btn-adicionar">Adicionar Cliente</button>
  </form>

  <table class="data-table">
    <thead>
      <tr>
        <th>ID</th>
        <th>Nome</th>
        <th>Ações</th>
      </tr>
    </thead>
    <tbody>
      {{#each clientes}}
        <tr>
          <td>{{this.id}}</td>
          <td>{{this.nome}}</td>
          <td>
            <a href="/clientes/{{this.id}}/editar" class="btn-editar">Editar</a>
            <form action="/clientes/{{this.id}}/excluir" method="POST" class="form-excluir">
              <button type="submit" class="btn-excluir">Excluir</button>
            </form>
          </td>
        </tr>
      {{/each}}
    </tbody>
  </table>

  <a href="/" class="btn-voltar"> Voltar</a>
</section>

<style>
  .page-content {
    padding: 50px 20px;
    text-align: center;
  }

  .form-cliente {
    margin-bottom: 20px;
  }

  .form-cliente input,
  .form-cliente button {
    padding: 10px;
    margin-right: 10px;
    border-radius: 4px;
    border: 1px solid #ccc;
  }

  table {
    margin: 30px auto;
    border-collapse: collapse;
    width: 85%;
    background-color: #fff;
    border-radius: 10px;
    overflow: hidden;
    box-shadow: 0 3px 10px rgba(0, 0, 0, 0.08);
  }

  th, td {
    padding: 14px 18px;
    border-bottom: 1px solid #eee;
    text-align: center;
  }

  th {
    background-color: #333;
    color: #fff;
    text-transform: uppercase;
    letter-spacing: 1px;
  }

  tr:hover td {
    background-color: #fdf8e6;
  }

  .btn-editar {
    background-color: #4CAF50;
    color: white;
    padding: 7px 14px;
    border-radius: 4px;
    text-decoration: none;
    transition: 0.3s;
  }

  .btn-editar:hover {
    background-color: #45a049;
    transform: scale(1.05);
  }

  .btn-excluir {
    background-color: #d9534f;
    color: white;
    padding: 7px 14px;
    border: none;
    border-radius: 4px;
    cursor: pointer;
    transition: 0.3s;
  }

  .btn-excluir:hover {
    background-color: #c9302c;
    transform: scale(1.05);
  }

  .form-excluir {
    display: inline;
  }

  .btn-voltar {
    display: inline-block;
    margin-top: 30px;
    background-color: #333;
    color: white;
    text-decoration: none;
    padding: 10px 20px;
    border-radius: 6px;
    transition: 0.3s;
  }

  .btn-voltar:hover {
    background-color: #ffd369;
    color: #000;
    transform: translateY(-2px);
  }
</style>